using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Config;
using Formula;
namespace Market.Logic.Domain
{
    public partial class S_KPI_Category
    {
        //public static List<S_KPI_Category> GetCurrentVersionCategories(IndicatorOrgType orgType)
        //{
        //    var entities = FormulaHelper.GetEntities<MarketEntities>();
        //    var state = YesOrNo.Yes.ToString();
        //    var orgtype = orgType.ToString();
        //    var list = entities.Set<S_KPI_Category>().Where(c => c.State == state && c.OrgType.Contains(orgtype)).OrderBy(c => c.SortIndex).ToList();
        //    return list;
        //}

    }
}
